import { Routes } from '@angular/router';
import { VenueSearchComponent } from './pages/venue-search/venue-search';

export const SEARCH_ROUTES: Routes = [
  { path: '', component: VenueSearchComponent },
];
